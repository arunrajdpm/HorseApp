import { ClearSymbolsPipe } from './clear-symbols.pipe';

describe('ClearSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
